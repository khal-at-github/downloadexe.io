#include <stdio.h>
#include <windows.h>

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow)
{
    printf("This is my first app running on the Windows operating system made using C programming language\n");
    system("pause"); // Wait for 20 seconds
    return 0;
}
